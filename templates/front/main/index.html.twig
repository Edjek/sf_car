{% extends 'front/base.html.twig' %}

{% block title %}Car
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			max-width: 800px;
			font: 18px / 1.5 sans-serif;
		}
	</style>
	<div class="example-wrapper container text-center mt-3">
		<h1>Bienvenue!</h1>

		<div class="mt-5">
			<div>
				<a href="{{ path('car_list') }}" class="btn btn-outline-dark w-50 m-2">Voir les véhicules</a>
			</div>
			<div>
				<a href="{{ path('brand_list') }}" class="btn btn-outline-dark w-50 m-2">Voir les marques</a>
			</div>
			<div>
				<a href="{{ path('group_list') }}" class="btn btn-outline-dark w-50 m-2">Voir les groupes</a>
			</div>
			<div>
				<a href="{{ path('image_list') }}" class="btn btn-outline-dark w-50 m-2">Voir les images</a>
			</div>
		</div>
	</div>
{% endblock %}
