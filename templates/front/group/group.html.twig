{% extends 'front/base.html.twig' %}

{% block title %}Group
{% endblock %}

{% block body %}

	<div class="container mt-3">

		<div>
			<a href="{{ path('main') }}" class="btn btn-outline-primary">
				Accueil
			</a>
			<a href="{{ path('group_list') }}" class="btn btn-outline-primary">
				Liste des groupes
			</a>
		</div>

		<div class="container border rounded w-50 p-5 mt-5">
			<h1>{{ group.name }}</h1>
			<p>{{ group.country }}</p>
		</div>
	</div>

{% endblock %}
